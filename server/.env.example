# Example environment file for Moodify Spring Boot (server)
# Copy this to `.env` and fill with your real secrets locally.

# Database
DB_HOST=your-db-host
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASS=change-me
DB_URL=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=disable

# OpenAI
OPENAI_API_KEY=sk-your-key
